PyYAML>=6.0
# Round-trip YAML — data/manual_publications.yml is rewritten in place
# when the overlay prunes itself, and has to keep its comments.
ruamel.yaml>=0.18
# refresh_citations.py only — safe to skip in CI where the citations
# cache is already committed.
requests>=2.31
beautifulsoup4>=4.12
